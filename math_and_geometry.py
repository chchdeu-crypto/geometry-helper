# triangle
while user_options != 6:
    match choose:
        case 1:
            base=input("enter base")
            height=input("enter height")
            result=base*height/2
            print(f"formula: base * height / 2/n the area is {result}")